2mov.ru {
        handle /admin* {
        reverse_proxy bot:8080
    }

    handle /webhookd* {
        reverse_proxy bot-driver:8080
    }
    handle /webhook* {
        reverse_proxy bot:8080
    }
    handle {
        root * /app/site
        file_server
    }
    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
    }
}
