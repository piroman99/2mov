abroad.2mov.ru {
    # Прокси вебхуков на Telegram-бота
    handle /webhook* {
        reverse_proxy localhost:8080
    }
    
    # Статика или заглушка
    handle {
        respond "2MOV Bot Webhook" 200
    }
}
